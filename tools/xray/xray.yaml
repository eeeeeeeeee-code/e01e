- name: x
  description: |-
    A command that enables all plugins.
    You can customize new commands or modify the plugins enabled by a command in the configuration file.
  enabled_plugins:
    - printer
    - service-scan
    - target-parser
    - vuln-scan
  disabled_plugins: []
  plugin_path:
    - ./tools/xray/plugin
  module_config: ./tools/xray/module.xray.yaml
  plugin_config: ./tools/xray/plugin.xray.yaml
